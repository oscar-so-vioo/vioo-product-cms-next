export enum UserRole {
    UNVERIFIED_USER = 'Unverified User',
    ROOT_USER = 'Root User',
    STUDENT = 'Student',
    ADMIN = 'Admin'
}

export enum TokenType {
    ACCESS = 'access_token',
    REFRESH = 'refresh_token',
}
